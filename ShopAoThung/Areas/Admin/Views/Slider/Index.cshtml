@model IEnumerable<ShopAoThung.Models.Mslider>

@{
    ViewBag.Title = "Slider";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
<!-- Content Wrapper. Contains page content -->
<div class="card card-title bg-white p-1">
    <div class="row">
        <div class="col-md-6 "> <h5 class="pl-2 mt-2 text-blues">Tất cả ảnh bìa</h5> </div>
        <div class="col-md-6 m-0">
            <ol class="breadcrumb bg-white float-sm-right m-0">
                <li class="breadcrumb-item m-0"><a href="~/Admin">Home</a></li>
                <li class="breadcrumb-item active m-0">Tất cả ảnh bìa</li>
            </ol>
        </div>
    </div>
</div>




<section >
    <div class=" bg-white card p-3 ">
        <div class="row justify-content-end">
            <div class="col-md-6 ">
                <div class=" pt-1 ml-5 float-right mr-3">
                    <a class="text-white btn-sm btn-success" href="~/admin/slider/Create">Add slider <i class="fas fa-plus"></i></a>
                    <a class="text-white btn-sm btn-danger " href="~/admin/slider/trash">Thùng rác<i class="fas fa-trash"></i></a>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-9  w-100 ">
                @Html.Partial("_MessgeAlert")
            </div>
            <div class="col-md-12 p-3">

                <table id="myTable1" class="table px-3 table-striped table-bordered dt-responsive nowrap">
                    <thead>
                        <tr>
                            <th>Hình</th>
                            <th width="30%">Tên slider</th>
                            <th>Ngày Tạo</th>
                            <th>Chức Năng</th>
                            <th>ID</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>

                                <td><img width="50px" src="~/public/images/bg-images/@Html.DisplayFor(modelItem => item.img)"></td>
                                <td> @Html.DisplayFor(modelItem => item.name)</td>
                                <td style="font-size:14px;">@Html.DisplayFor(modelItem => item.created_at)</td>
                                <td>
                                    @if (item.status == 1)
                                    {
                                        <a class="btn btn-sm btn-light text-success border a33" href="@Url.Action("Status", "Slider", new { id = item.ID })"> <i class="fas fa-toggle-on"></i> </a>
                                    }
                                    else
                                    {
                                        <a class="btn btn-sm btn-light text-danger border a33" href="@Url.Action("Status", "Slider", new { id = item.ID })"> <i class="fas fa-toggle-off"></i> </a>
                                    }


                                    <a class="btn btn-sm btn-light text-danger border" href="@Url.Action("Deltrash", "Slider", new { id = item.ID })"> <i class="far fa-trash-alt"></i></a>
                                    <a class="btn btn-sm btn-light text-warning border" href="@Url.Action("Edit", "Slider", new { id = item.ID })">
                                        <i class="far fa-edit"></i>
                                    </a>
                                </td>
                                <td class="text-cam ">@Html.DisplayFor(modelItem => item.ID)</td>
                            </tr>
                        }
                    </tbody>

                </table>
            </div>
        </div>
    </div>
</section>
<script>
    $("#slider").addClass("active");
</script>
